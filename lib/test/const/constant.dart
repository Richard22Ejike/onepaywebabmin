import 'package:flutter/material.dart';

const cardBackgroundColor = Color.fromARGB(255, 55, 2, 77);// Corresponds to globalvariable.backgroundcard
const primaryColor = Color.fromARGB(255, 137, 4, 181); // Corresponds to globalvariable.primarycolor
const secondaryColor = Color(0xFFFFFFFF); // Remains white
const backgroundColor = Color.fromARGB(255, 254, 254, 254); // Corresponds to globalvariable.backgroundcolor
const selectionColor = Color.fromARGB(255, 157, 46, 193);

const defaultPadding = 20.0;
